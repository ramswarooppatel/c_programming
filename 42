// Category: Functions
// Question 2: Write a C program to calculate the power of a number using recursion.
#include <stdio.h>
int power(int base, int exp) {
    if (exp == 0) return 1;
    return base * power(base, exp - 1);
}
int main() {
    int base = 2, exp = 3;
    printf("%d", power(base, exp));
    return 0;
}
// RPX~PYCHUNK @ C
