// Category: Pointers
// Question 3: Write a C program to find the maximum of two numbers using pointers.
#include <stdio.h>
int max(int *x, int *y) {
    return (*x > *y) ? *x : *y;
}
int main() {
    int a = 10, b = 20;
    printf("Max: %d", max(&a, &b));
    return 0;
}
// RPX~PYCHUNK @ C
