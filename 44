// Category: Functions
// Question 4: Write a C program to find the sum of digits of a number using recursion.
#include <stdio.h>
int sum_of_digits(int n) {
    if (n == 0) return 0;
    return n % 10 + sum_of_digits(n / 10);
}
int main() {
    int n = 123;
    printf("%d", sum_of_digits(n));
    return 0;
}
// RPX~PYCHUNK @ C
