// Write a C program to concatenate two strings.
//16
#include <stdio.h>
#include <string.h>
int main() {
    char str1[50] = "Hello ";
    char str2[] = "World!";
    strcat(str1, str2);
    printf("%s", str1);
    return 0;
}
// RPX~PYCHUNK @ C
