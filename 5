#include <stdio.h>
//ramswaroop
int main() {
    float pi=3.14159,r;
    printf("Enter the radius of the circle :");
    scanf("%f",&r);
    printf("Area of circle: %f",pi*r*r);
    printf("\ncircumference of circle: %f",2*pi*r);
    return 0;
}
